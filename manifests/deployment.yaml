apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: jekyllapp
spec:
  selector:
    matchLabels:
      app: jekyllapp
  replicas: 3
  template:
    metadata:
      labels:
        app: jekyllapp
    spec:
      containers:
      - name: jekyllapp
        image: jacopen/jekyllapp:TAG
        resources:
          requests:
            cpu: 100m
            memory: 500Mi
        env:
        - name: ENV
          value: production
        ports:
        - containerPort: 8080
